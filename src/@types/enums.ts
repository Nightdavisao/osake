export enum MKPlaybackState {
    Playing = "playing",
    Paused = "paused",
    Stopped = "stopped",
}

export enum MKRepeatMode {
    None = "none",
    One = "one",
    All = "all",
}

export type WebsiteType = 'music' | 'classical'
